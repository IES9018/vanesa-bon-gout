# Copiar a `.env` (mismo directorio, NO se versiona) y completar valores reales.
# Nunca commitear el `.env` con secretos (ver .opencoderules, seccion 3).
PORT=3001
DB_NAME=bongout
DB_USER=bongout
DB_PORT=5432
DB_PASSWORD=CAMBIAR_password_seguro_aqui
JWT_SECRET=CAMBIAR_jwt_secret_largo_y_aleatorio_aqui
FRONTEND_PORT=5174
VITE_API_URL=http://localhost:3001
